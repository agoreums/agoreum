# Reown AppKit pulls a transitive @wagmi/core that lists optional peers
# (permissionless, privy) which want an older `ox` than wagmi v3 uses. Those
# packages are never loaded here, so their optional peer ranges should not block
# resolution. `npm ci` installs exactly from the lockfile regardless.
legacy-peer-deps=true
